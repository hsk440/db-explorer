[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
markers =
    integration: requires real PostgreSQL database (set DB_* env vars)
    integration_api: requires real Anthropic API key (set ANTHROPIC_API_KEY)
addopts = -v --tb=short -m "not integration and not integration_api"
